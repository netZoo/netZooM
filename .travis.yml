language: c

addons:
  apt:
    pacakges:
    - octave

matrix:
  include:
  - os: windows
  - os: linux
    language: sh
    before_install:
    - echo -ne '\n' | sudo apt-add-repository ppa:octave/stable
    - sudo apt-get update
    - sudo apt-get install octave -y
  - os: osx
    before_install:
    - brew install octave > /dev/null

script:
- octave-cli tests/testNetzoo.m
