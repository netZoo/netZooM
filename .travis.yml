language: c

addons:
  apt:
    pacakges:
    - octave

matrix:
  include:
  - os: linux
  - os: windows
    language: sh
    before_install:
    - apt-get install octave
  - os: osx
    before_install:
    - brew install octave > /dev/null

script:
- octave-cli tests/testNetzoo.m
