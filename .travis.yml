language: c

addons:
  apt:
    pacakges:
    - octave

matrix:
  include:
  - os: osx
    before_install:
    - brew update
    - brew install octave > /dev/null

script:
- travis_wait octave-cli tests/testNetzoo.m
